# Local dev overrides (this file is gitignored).
# Skip the login screen entirely for local development.

[auth.anonymous]
# Allow unauthenticated access...
enabled = true
# ...as an Admin so you can do everything without logging in.
org_role = Admin

[auth]
# Hide the built-in login form so there is no login screen at all.
disable_login_form = true
